// const express = require("express");
// const router = express.Router();
// const { createOrder, getOrders } = require("../controllers/orders");
// const verifyToken = require("../middleware/authen/verifyToken");

// router.post("/", verifyToken, createOrder); // Authenticated users can create an order
// router.get("/", verifyToken, getOrders); // Authenticated users can view their orders

// module.exports = router;
